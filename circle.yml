machine:
  node:
    version: 6.10.0
dependencies:
  pre:
    - npm install angular-cli@latest -g
## Customize test commands
test:
  override:
    - npm run notest
    #- ng lint
    #- ng test --watch false # single test run